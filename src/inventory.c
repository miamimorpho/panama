int
inventoryAdd(void)
{

	return 0;
}

/*
int
itemMoveToContainer(struct Dungeon* d, Handle obj, Handle in){
	struct Items *items = d->items;
	vec16 pos;
	if(spacePos(items->lookup, obj.id, pos)){
		return 1;
	}
	spaceRemove(items->lookup, obj.id);
	//todo add to inventory
	items->container_host[obj.id] = in;

	return 0;
}
*/
